name: $(Build.BuildId).$(Year:yy)$(DayOfYear)
trigger: { branches: { include: [ master ] }, paths: { include: [ /projects/audacia/date-parse-interceptor ] } }

jobs:

- template: /build.template.yaml
  parameters:
    project: date-parse-interceptor
    test: false

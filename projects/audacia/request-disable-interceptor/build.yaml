name: $(Build.BuildId).$(Year:yy)$(DayOfYear)
trigger: { branches: { include: [ master ] }, paths: { include: [ /projects/audacia/request-disable-interceptor ] } }

jobs:

- template: /build.template.yaml
  parameters:
    project: request-disable-interceptor
    test: false
